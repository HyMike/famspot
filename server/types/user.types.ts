export interface UserData {
    name: string, 
    email: string,
    zipCode: string,
    ageRange: string
}

export interface UserPromptData {
    id: number,
    zipCode: string, 
    ageRange: string
}